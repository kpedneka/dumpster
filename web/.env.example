# No environment variables are required for the web frontend.
# In development, Vite proxies /api → http://localhost:8080 (see vite.config.ts).
# In production, the API is served from the same origin via session cookies.
